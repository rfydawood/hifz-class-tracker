# Build output (regenerated by Gradle, not source)
/build/
/app/build/
.gradle/
local.properties

# Signing key and password - NEVER commit these to a public repo.
# Keep android.keystore and its password backed up somewhere private instead.
android.keystore
KEYSTORE_PASSWORD_KEEP_SAFE.txt

# Built app files - distributed via GitHub Releases / Firebase, not committed here
*.apk
*.apk.idsig
*.aab
