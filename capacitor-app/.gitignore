# Dependencies (reinstall with npm install)
node_modules/

# Build output (regenerated by Gradle, not source)
/android/build/
/android/app/build/
/android/.gradle/
/android/local.properties
/android/app/release/
/android/app/*-aligned.apk

# Built app files - distributed via Firebase App Distribution, not committed here
*.apk
*.apk.idsig
*.aab
